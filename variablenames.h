#ifndef VARIABLENAMES_H
#define VARIABLENAMES_H

/*
 * Este análisis se hará mediante una blacklist, se buscará todas las palabras de la blacklist en todos los archivos de entrada.
*/

int searchVariableNamesMistakes();

struct BlackList{

};


#endif // VARIABLENAMES_H
